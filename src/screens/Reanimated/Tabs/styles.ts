import { StyleSheet } from 'react-native'

export const tabsStyles = StyleSheet.create({
  container: {
    flex: 1,
  },
  imageBackground: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  heartImage: {
    resizeMode: 'center',
  },
})
